[project]
name = "utsikt-dataprodukt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-bigquery>=1.12.0",
    "dbt-core-experimental-parser==2.0.0a4"
]

[tool.ruff]
# Exclude a variety of commonly ignored directories.
exclude = [
    ".sqlfluff"
    ]

[dependency-groups]
dev = [
    "loguru>=0.7.3",
    "ruff>=0.16.1",
    "sqlfluff>=4.3.0",
    "sqlfluff-templater-dbt>=4.3.0",
    "toml-to-requirements>=0.3.0",
]
