[project]
name = "utsikt-dataprodukt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-bigquery>=1.9.2",
]

[tool.uv.workspace]
members = [
    "dbt_utsikt",
]
